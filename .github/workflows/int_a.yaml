name: Integration A workflow

on:
  workflow_call:
    inputs:
      path:
        required: true
    paths:
      - ${{ inputs.path }}

runs:
  using: "composite"
  steps:
    - name: Say hi from path
      run: echo "hello from ${{ inputs.path }}"
      shell: bash
